# Google Tag Manager Container ID (public tracking ID, not a secret).
# Leave unset to ship without analytics. When set, the cookie-consent banner
# gates the actual GTM script load on visitor opt-in.
NEXT_PUBLIC_GTM_ID=GTM-5CXC3C8
